# 全面审查报告

更新时间：2026-03-11

---

## 1. 审查范围
这次按“整套 `sourceDocs/` 是否足以直接支撑代码开发”来审，不只看单条链。

覆盖范围：
1. `总览 / 产品规范 / 实施与验收 / 运行与治理 / 核心设计 / 研究与讨论 / 模板与附录`
2. `sourceDocs/` 全量 Markdown 文档，共 `98` 份
3. 仓库内对 `sourceDocs/` 的治理测试与关键引用

采用方法：
1. 目录角色与阅读链审查
2. 顶层正式对象与 `核心设计/` owner 对账
3. transport / carrier / domain 分层审查
4. 生命周期闭环审查
5. 验收 / 案例 / 样例 / 模板一致性审查
6. 仓库引用与治理测试盲区审查

---

## 2. 结论
这份报告最初记录的是一次“冻结后总审计”的阻断结论。  
在随后的收敛轮次里，报告中列出的阻断项已经被系统性修复。

当前结论更新为：

**这套文档已经可以作为下一步最小正式代码开发基线使用。**

补充说明：
1. 之前围绕 8 个治理深化点形成的阶段性冻结决策，现已吸收回正式 owner 文档与实施主线。
2. 本报告继续保留审计结论与修复脉络，但不再承担这些设计点的现行入口角色。

现在的边界是：
1. `sourceDocs/` 已经足够支撑：
   - `coding_pack / COMPLEX` 最小闭环实现
   - `RiskAssessment / TaskScopeSnapshot / ExecutionAuthorization` 这条安全边界
   - `PendingDecisionWindow` 家族、review/result、durable outbox 主链
2. 仍然值得继续深化的内容，主要是治理增强层的后续设计，不再是主链阻断项。
3. 实现时应以最新主线文档为准，而不是以本报告里的历史阻断描述为准。

---

## 3. 机械结果
当前机械结果是稳定的：
1. `sourceDocs/` 断链 `0`
2. 缺 `更新时间` 的文档 `0`
3. `documentation-alignment.test.ts` 通过，`3/3`
4. `workflow-engine.test.ts` 通过，`16/16`

这轮审计确认：
1. 目录结构、中文命名、阅读链、旧路径清理都已基本收住
2. 现有问题主要是**语义漂移**，不是文件迁移残留

---

## 4. 文档职责矩阵
| 目录 | 允许定义什么 | 不应定义什么 | 审查结果 |
| --- | --- | --- | --- |
| `总览/` | 产品入口、术语、阅读链、真相源边界 | 字段 contract、接口草图、实现切片 | 已稳定，术语层级已与正式对象总表对齐 |
| `产品规范/` | 生命周期、对象关系、运行时语义、状态机、链路图 | Rust 形状建议、宿主接线细节 | 已稳定，剩余工作主要是实现前测试写实 |
| `实施与验收/` | 实施顺序、验收规则、测试案例、开工指引 | 发明新对象、改写正式 contract | 已稳定，当前继续承担第一条 spike 的实现前测试基线 |
| `运行与治理/` | 运行时基线、决策索引、治理审计材料 | 代替产品规范或核心合同 | 角色已收稳，历史材料已降级成审计记录 |
| `核心设计/` | 正式 contract、对象边界、宿主接入、架构切线 | 顶层产品入口文案 | 正式 owner 已基本齐备，剩余是 pack/domain 深化 |
| `研究与讨论/` | 背景、吸收来源、取舍讨论 | 正式规范 owner | 只保留讨论与禁用 noun 清单，不再承担 live owner 角色 |
| `模板与附录/` | 可复用模板、附录说明 | 主线真相源 | 当前基本未反向污染主线 |

---

## 5. 已稳定部分
这次重审后，可以确认已经比较稳定的部分有：
1. 宿主输入三层边界：
   `HostInboundTurn -> CurrentTurnEnvelope`
   `HostSemanticBundle -> SemanticDecisionEnvelope`
   `ControlActionIngress -> ControlAction`
2. 三条决策窗口主链：
   `StartCandidate / ApprovalRequest / BoundaryConfirmation`
   已在 `运行时语义 / 验收规范 / 测试案例 / 决策窗口合同` 中统一成
   “先开窗口，再投影，再发 payload，再用 `decision_token` 消费”
3. `workflowStage` 与 `PhasePlan / StagePackageId` 的层级边界
4. `managedTaskClass` 的 wire/domain 双口径：
   宿主 wire 用 `complex / huge / max`
   Loom domain 用 `COMPLEX / HUGE / MAX`
5. durable outbox 与 `ack_outbound` 的主链
6. `ProofOfWorkBundle -> ResultContract -> ResultSummaryPayload` 的证据关系

---

## 6. 已关闭阻断回顾
本报告最初记录的阻断项，当前已全部完成收敛关闭。

已关闭的阻断主题包括：
1. 决策窗口家族层级写反
2. `watchdogNoticeState` 缺 owner 与泛化升级策略名污染
3. `ResultSummaryPayload` 顶层字段形状漂移
4. 实施主文档混写 transport / domain / payload / delivery
5. `taskConversationPolicy=chat_open` 悬空成疑似 runtime object

## 7. 当前开放问题与非阻断深化主题
当前已经没有阻断“开始第一条最小代码实现”的一致性问题。

当前剩余的是非阻断型深化主题：
1. `RiskAssessment` 的最小实现时机与升级时机，需要在代码前测试里进一步写实。
2. `TaskScopeSnapshot` 的工程上下文字段，后续仍可按 pack/domain 继续细化。
3. `watchdogNoticeState` 的轻量注意力治理字段已经到位，但用户级偏好语言还没有进入正式设计。

## 8. 是否可以开始开发
可以开始开发，但应按收敛后的主线入口开工：
1. [目标规格.md](../产品规范/目标规格.md)
2. [运行时语义.md](../产品规范/运行时语义.md)
3. [产品实施总方案.md](../实施与验收/产品实施总方案.md)
4. [测试案例.md](../实施与验收/测试案例.md)
5. [目录说明.md](../核心设计/目录说明.md)

不建议再以本报告早期段落里的阻断描述作为当前依据。  
当前开发前还值得补的，不是“再修主链定义”，而是把第一条代码实现线的测试基线继续写实。

---

## 9. 验证记录
本轮最终验证结果为：
1. `sourceDocs/` 全量链接检查
2. `sourceDocs/` 全量 `更新时间` 检查
3. 顶层对象与核心合同对账
4. 典型链路抽查：
   - candidate -> start card -> approve_start
   - review -> escalate_to_user -> approval window
   - active task -> second task -> boundary confirmation
   - result -> outbox delivery -> ack
5. 治理测试回归：
   - `documentation-alignment.test.ts`
   - `workflow-engine.test.ts`

---

## 10. 下一步建议
下一步建议不再继续补主链名词，而是进入“实现前测试写实”：
1. 固定 `coding_pack / COMPLEX` 下 `RiskAssessment` 的生成时机
2. 固定 `TaskScopeSnapshot` 工程上下文字段在 spike 中的最小形状
3. 固定 `ExecutionAuthorization` 如何受 `TaskScopeSnapshot` 和 `RiskAssessment` 联合收紧
4. 再进入 `/Users/codez/.openclaw/loom/` 的真实代码骨架实现
